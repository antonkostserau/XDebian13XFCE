# XDebian13XFCE

To configure the NTP protocol, execute:
timedatectl set-ntp true

To add a user to the sudo group, execute:
sudo usermod -aG sudo user

To add a user to the docker group, execute:
sudo usermod -aG docker user
